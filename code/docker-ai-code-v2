FROM quantumgears/ai-dev:flask-base-v1

ENV BASE_DIR  "/ai-prod/quanta/"
#RUN mkdir $BASE_DIR

RUN buildDeps='gcc libc6-dev python3-dev g++ git python-pip' \
    && apt-get update \
    && apt-get install -y $buildDeps --no-install-recommends \
    && rm -rf /var/lib/apt/lists/* \
    && git clone https://b3a9f6fcb9085ca1527a3f7b19400523898a700c:@github.com/rmallal/ai-prod.git \
    && apt-get purge -y --auto-remove $buildDeps

WORKDIR $BASE_DIR
EXPOSE 8080
